# ERROR RECORD — Nosok v12

## أخطاء مغلقة
- لا يوجد compile blocker جديد معروف داخل هذه الجلسة.

## مخاطر مفتوحة
1. تنفيذ billing الحقيقي يعتمد على عقود `billing_system` داخل PalWakf الكاملة.
2. unit scope الفعلي يجب أن يأتي من `AccessProfile` + `core.org_units` عند الدمج.
3. Role UAT يحتاج أدلة Browser فعلية قبل production approval.

## آخر baseline مستقر قبل هذه الدفعة
Nosok v11 — Production Runtime Batch.
